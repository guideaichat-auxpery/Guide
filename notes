Allow for custom student username and password

Newly generated accounts aren't being saved so each time the page refreshes I have to regenerate a new account.

Once I input a username, it sends me to a new tab. this needs a fix